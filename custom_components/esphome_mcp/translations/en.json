{
  "config": {
    "step": {
      "user": {
        "title": "ESPHome MCP",
        "description": "Run an ESPHome-focused MCP server inside Home Assistant and expose it through a Home Assistant webhook. The webhook works through Nabu Casa Remote UI or any reverse proxy."
      }
    },
    "abort": {
      "already_configured": "ESPHome MCP is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ESPHome MCP",
        "description": "Configure the ESPHome MCP server. Open the [ESPHome MCP panel](/esphome-mcp) for status.\n\n{connect_url}",
        "data": {
          "server_port": "MCP server listening port",
          "bind_host": "Network access",
          "webhook_auth": "Authentication mode",
          "enable_webhook": "Remote access via webhook",
          "external_url": "External URL (optional)",
          "webhook_id_override": "Custom webhook secret (optional)",
          "secret_path_override": "Custom direct-access path (optional)",
          "pip_spec": "Developer: FastMCP package override",
          "regenerate_secrets": "Regenerate connect secrets now"
        },
        "data_description": {
          "server_port": "The port this server listens on. The default is 9590 so it can run alongside ha-mcp.",
          "bind_host": "Who can connect to the MCP server port directly. The webhook URL and sidebar panel work even when loopback is selected.",
          "webhook_auth": "How MCP clients authenticate at the webhook URL. With the secret URL, the link itself is the credential. With Home Assistant sign-in, clients authenticate through Home Assistant OAuth.",
          "enable_webhook": "Turn off for local-only mode. The direct server port and sidebar panel keep working.",
          "external_url": "Shown as the primary connect URL. Use this when Home Assistant sits behind your own domain or reverse proxy.",
          "webhook_id_override": "Replaces the random webhook secret in the connect URL (/api/webhook/...). Use a long, hard-to-guess value.",
          "secret_path_override": "Replaces the random path used for direct access on the server port.",
          "pip_spec": "Leave empty. Only for testing a specific FastMCP build; overrides the default pinned server dependency until cleared.",
          "regenerate_secrets": "One-time action: mints a fresh random webhook secret and direct-access path."
        }
      }
    }
  },
  "issues": {
    "server_start_failed": {
      "title": "The ESPHome MCP server failed to start",
      "description": "The ESPHome MCP server could not be started inside Home Assistant:\n\n{detail}\n\nCheck the Home Assistant logs, then reload the integration to retry."
    },
    "server_package_import_failed": {
      "title": "The ESPHome MCP server package could not be imported",
      "description": "Importing a dependency for the ESPHome MCP server failed:\n\n{detail}\n\nCheck the Home Assistant logs and reload the integration to retry."
    }
  },
  "selector": {
    "server_webhook_auth": {
      "options": {
        "none": "Secret webhook URL (default)",
        "ha_auth": "Sign in with Home Assistant (OAuth)"
      }
    }
  }
}
